# what-to-eat-today
An upgraded food randomizer that uses country-based categories and suggests a meal combo (main + drink). Data is persisted into a CSV file so users can review what was suggested previously.
